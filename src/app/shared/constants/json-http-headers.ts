import { HttpHeaders } from '@angular/common/http';

// eslint-disable-next-line @typescript-eslint/naming-convention
export const JSON_HTTP_HEADERS = new HttpHeaders({
  'Content-Type': 'application/json',
});
